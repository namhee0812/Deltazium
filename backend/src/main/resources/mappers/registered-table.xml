<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.deltazium.backend.registration.RegisteredTableRepository">

    <resultMap id="registeredTable" type="io.deltazium.backend.registration.RegisteredTable">
        <constructor>
            <idArg column="id" javaType="long"/>
            <arg column="schema_name" javaType="String"/>
            <arg column="table_name" javaType="String"/>
            <arg column="source_connection_id" javaType="_long"/>
            <arg column="target_connection_id" javaType="_long"/>
            <arg column="target_schema_name" javaType="String"/>
            <arg column="target_table_name" javaType="String"/>
            <arg column="snapshot_mode" javaType="String"/>
        </constructor>
    </resultMap>

    <select id="findAll" resultMap="registeredTable">
        SELECT * FROM registered_tables ORDER BY schema_name, table_name
    </select>

    <select id="countBySchemaAndTable" resultType="_int">
        SELECT COUNT(*) FROM registered_tables
         WHERE schema_name = #{schema} AND table_name = #{table}
    </select>

    <select id="countTableNameInOtherSchema" resultType="_int">
        SELECT COUNT(*) FROM registered_tables
         WHERE table_name = #{table} AND schema_name &lt;&gt; #{schema}
    </select>

    <insert id="insertRow" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO registered_tables
          (schema_name, table_name, source_connection_id, target_connection_id,
           target_schema_name, target_table_name, snapshot_mode)
        VALUES (#{schemaName}, #{tableName}, #{sourceConnectionId}, #{targetConnectionId},
                #{targetSchemaName}, #{targetTableName}, #{snapshotMode})
    </insert>

    <delete id="deleteRow">
        DELETE FROM registered_tables WHERE id = #{id}
    </delete>

</mapper>
